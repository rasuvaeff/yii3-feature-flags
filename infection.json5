{
    "$schema": "vendor/infection/infection/resources/schema.json",
    "source": {
        "directories": ["src"]
    },
    "logs": {
        "text": "php://stderr",
        "summary": "build/infection-summary.log"
    },
    "phpUnit": {
        "configDir": "."
    },
    "testFramework": "testo",
    // 97, not 100: three equivalent mutants remain. No ignores.
    // - FeatureFlags::__construct Coalesce on ?FlagEvaluator: the injected
    //   evaluator is behaviorally identical to the default (FlagEvaluator is
    //   final and PercentageRollout is final/stateless), so the swap is
    //   unobservable.
    // - PercentageRollout::isEnabled === 0 / === 100 guards: pure short-circuits;
    //   the hash path yields bucket<0 (always false) / bucket<100 (always true)
    //   at the boundaries, producing the same result.
    "minMsi": 97,
    "mutators": {
        "@default": true
    }
}
